# 参考附件的历史实现状态

> 历史记录：其中的独立实验内核和命令已在 0.4.0 移除，不代表当前架构或可用入口。当前实现见 [代码清理说明](CODE-CLEANUP-0.4.0.md)。

本文保留附件参考包在迁入前的自述，下面的“上游”“本包”及文档优先级仅适用于原参考包。它不定义当前 EcologyRSI-DSH 的实现状态，也不构成本项目的执行指令。当前主链、已迁入能力与交付边界以 [0.3.59 整体交付说明](FINAL-DELIVERY-0.3.59.md) 为准；逐步迁移的过程见 [实施记录](IMPLEMENTATION-2026-09-06.md)。

以下为原始参考状态。

## 已实现，并纳入离线端到端运行

| 能力 | 代码入口 | 已验证边界 |
|---|---|---|
| 不可变 JSON、Unicode 规范化、严格数值 | `kernel/identity.py` | 输入/输出嵌套修改不改变身份；重复键、NaN/Infinity 拒绝 |
| Genome、版本/行为/编译身份 | `kernel/genome.py` | 说明和谱系不伪造新行为；未知和无效参数拒绝；单轴修改检查 |
| 固定程序注册与编译 | `compile_program` | 只编译本地三类预测器；不执行生成代码 |
| 分离训练与推理视图 | `science/data.py` | 完整 episode 分区互斥；推理特征只读预测起点及以前；尺度只由训练数据计算 |
| 实际数值拟合 | `science/models.py` | 持续性、季节性、每个目标/时距单独岭回归；训练产物持久化 |
| 可信评分与配对筛选 | `science/evaluation.py` | 预测矩阵精确覆盖；重算聚合/单元指标；固定种子；区块数量不足不晋级 |
| 追加证据与原子晋级 | `storage/ledger.py` | 幂等先于状态检查；当前冠军比较绑定；CAS；撤销/过期；事务故障回滚 |
| 内容寻址与审计 | `storage/artifacts.py`、`Ledger.verify` | 哈希校验、事件链、冠军投影和报告投影核对 |
| 有界搜索、正负经验和档案 | `research/search.py`、`services.py` | 共享有限候选空间；经验包含假设与证据；档案不混上下文 |
| 本地评测预算 | `Ledger.reserve` | 执行前预留；重复请求不得再次启动；失败仍保守消耗一个评测预算 |
| 命令行、CSV 与固定策略基准 | `__main__.py` | 离线运行、拒绝覆盖旧输出、同种子科学结果复现、合成来源标识 |

## 有可运行组件测试，但尚未形成生产端到端能力

| 组件 | 已有部分 | 尚需完成 |
|---|---|---|
| `execution/jobs.py` | 真实 SQLite 租约、续约、取消、fencing、重复完成检查 | 接入作业状态机、远程 submit 对账、预算状态与运行恢复；当前 demo 不使用该队列 |
| `execution/backends.py` | 端口 Protocol、request/context/compiled 绑定、拒绝外部报分 | 使用本地上游实际 DSH 调用完成 transport；真实 submit/poll/cancel/lookup 合同验证 |
| 研究档案与经验 | 当前运行内的有限策略和持久化经验 | 跨任务检索、适用范围验证、能力撤销、技能抽象、外层策略实验 |

## 仅有详细设计/任务，尚未实现

完整 `ResearchPolicyGenome`、方法层自进化、跨任务外层 holdout、独立正式确认凭证、人工发布批准、业务级历史回滚、跨 run 样本暴露授权、运行恢复调度、多保真搜索、远程 provider 预算/限流、生成代码沙箱、原生 DSH 编译器整合、AGC 原始数据适配、上游数据库迁移及 UI/API 收口。

对应接口、数据结构、约束和验收分别在 ARCHITECTURE、MIGRATION、DSH_INTEGRATION、CODEX_PLAN 中。

## 上游整合状态

**尚未改动上游仓库，也未运行其完整测试。** 本次通过网页核对源码，在独立参考包中实现和测试。不能把本包的测试数与上游已有测试数相加，也不能据此把 T00—T19 标为 completed。

## 文档解释优先级

1. 本文件决定“现已实现什么”。
2. CURRENT_CODE 决定“本包实际可调用的接口是什么”。
3. ARCHITECTURE / MIGRATION 决定“上游最终应达到什么结构”。
4. CODEX_PLAN 决定“怎样逐阶段实现并验收”。

目标方案比参考基线更丰富是有意安排：先交付可运行的内核，再让本地 Codex 在真实上游环境中完成接入，不以更多空类冒充完整实现。
