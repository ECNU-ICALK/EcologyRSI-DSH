# 0.7.9 网页实跑验证

本版继承 [0.7.8 稳定性修复](WEB-EVOLUTION-0.7.8.md)，为独立样本评审统一设置 4096 Token 输出上限，并通过独立 DSH provider 配置使用 Kimi K3 的 low 推理档位。GLM 5.2 预测 Agent 仍自主选择数值工具和预测方法；宿主负责权限、因果边界、数值合法性、评分与科学晋升条件。

## 实际运行

从正常 DSH 侧栏进入工作台，完成真实模型预检并创建运行 `run:978671ea-fa44-4a97-8f06-61a06ed72ca5`。网页预览地址为 `http://127.0.0.1:8851/`，对应后端为 8879。未拦截网页请求、替换模型输出或用预录数据代替 Agent 调用。

截至 2026-09-07T17:54:05+00:00：

- 四个候选已生成。首个候选完整完成 64 个起点、576 个预测单元的初筛并正常结算；初筛得分 -0.0629098547、物理约束违规 0，科学门槛未通过，系统没有把它标为改进方案。
- 全运行累计完成 76 个起点、684 个评分单元，全部成功且具备完整 Agent 链路，宿主代预测次数为 0。
- 独立评审已接受 8 次输出；实际会话确认 low 推理和 4096 Token 生效，抽查前两次分别用 8 秒、10 秒完成。
- 数值工具调用共 360 次：成功 359 次，失败 1 次。唯一失败为 Agent 混用两类模型的参数键，宿主拒绝执行；Agent 随后使用有效工具证据完成该起点的全部 9 个预测，没有引用失败结果，也没有触发运行中止。
- 观察窗口中没有 DSH 子任务失败、样本身份拒绝、供应商限流或输出额度耗尽。结果持续写入账本，运行继续处理其他候选。

这些结果验证当前执行链路及工具错误后的自主恢复，尚不等同于完整进化、独立保留集或稳定预测提升已获验证。当前任务最大计划工作量为 1702 个候选预测起点（含重复推理），后续阶段仍由后台自动推进。工具调用成功也不代表预测准确。

## 工程验证

- Python 3.12.13：117 个模块、1507 项测试通过；真实 AGC 数据启用，无跳过。原生评审预算断言更新后，单独重跑对应 24 项集成测试并通过。
- DSH 插件：218 项测试通过；前端与代理：13 项测试通过，页面静态 smoke 通过。
- 正常网页六个工作区，1480px 桌面和 390px 手机共 12 项视图检查：无页面错误、意外 HTTP 错误和页面级横向溢出；数据页每页 20 行；该次检查的接口响应最长 59ms。
- 从隔离的干净源码快照构建 wheel、sdist、DSH 插件和完整交付包；产物校验及独立临时环境中的安装、CLI、健康接口、页面静态资源 smoke 通过。构建提交属于隔离快照，原项目工作区的未提交修改予以保留。

推理配置依据及可复用示例见 [原生推理配置](../../examples/dsh-native-inference.md)。原始会话、服务凭据、数据库和实验数据保留在本机运行目录，不进入交付包。
