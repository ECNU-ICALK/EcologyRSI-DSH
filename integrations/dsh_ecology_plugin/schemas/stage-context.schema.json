{
  "$id": "ecology-stage-context@1",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "stage_context_digest", "payload"],
  "properties": {
    "schema_version": {"const": "ecology-stage-context@1"},
    "stage_context_digest": {"type": "string", "pattern": "^[0-9a-f]{64}$"},
    "payload": {"type": "object"}
  }
}
